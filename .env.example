# No root `.env` is required for `pnpm dev` or `pnpm verify:all`.
# The repository compose files contain loopback-only synthetic credentials;
# hosted URLs/tokens belong only in provider secret stores.
#
# Optional manual compose namespace override:
# COMPOSE_PROJECT_NAME=tab10-dev
