# Handoff — recheck evidence correction

- Use this sibling package together with, and as a narrow interpretive correction to, `components-recheck/**`.
- Do not cite the 1440 or 360 contextual PNG as showing query/popup/active option.
- Keep `F-CMP-001` from raw DOM evidence.
- Describe `F-CMP-002` candidate evidence as: active option below the 1440 viewport; exact fixed-nav intersection and 360 visual state not revalidated.
- Ignore the derived `occludedByNav=true` boolean; exact rectangles do not intersect.
- Leave replacement visible active-option/nav capture to the coordinator pilot.
- No finding IDs, priorities, app files, canonical docs or original manifests were changed.

