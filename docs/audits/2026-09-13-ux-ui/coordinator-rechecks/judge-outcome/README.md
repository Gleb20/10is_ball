# Независимое воспроизведение координатора — потеря ответа очка

ROOT-JUDGE-OUTCOME-01: REPRODUCED на принятой базе из323файлов, Chromium153,390×844. Данные созданы заново на собственном одноразовом PostgreSQL; приватные данные исполнителя не использовались. Приложение не изменялось.

Сервер успешно применил первый POST, после чего браузерный ответ намеренно оборван. GET содержит точный ключ запроса, экран показывает1:0 и инструкцию «проверьте счёт и повторите». Координаторский driver следует этой инструкции новым нажатием: второй ключ принят, счёт2:0. Это риск повторного пользовательского действия, а не доказательство автоматической двойной записи.

[Receipt](receipt.json), [ошибка и1:0](lost-response-390.png), [результат повтора2:0](following-repeat-390.png), [cleanup](cleanup.json). Оба кадра открыты координатором через view_image. Частота естественных сбоев и ошибки реального пользователя не измерялись.

Replay: replay.mjs принимает private env TAB10_E2E_BASE_URL/E2E_ADMIN_EMAIL/E2E_ADMIN_PASSWORD. Нужен собственный одноразовый environment=test с миграциями/runtime role и собранной принятой версией. Для другого checkout обновить абсолютные import/output пути. Credentials и connection strings не сохранять и не передавать в соседние задачи. Генератор окружения расположен отдельно; новые credentials случайные.

Первая попытка подготовки остановилась до браузера из-за пропущенного NODE_ENV=test для migration runner; это ошибка подготовки в attempt-01.json, cleanupPASS. После исправления только окружения: ранее успешная сборка не повторялась, migration foundation9/9, воспроизведение PASS, собственные API/web/PG полностью очищены. Новый полный gate1257 для этого исследовательского прогона не заявляется.
