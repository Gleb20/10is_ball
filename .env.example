# Local Docker PostgreSQL only. Copy to .env before the first `docker compose up`.
# Keep the same values in apps/api/.env DATABASE_URL. Never reuse these values
# in Neon, Render, Vercel, or any shared environment.
TAB10_POSTGRES_USER=tab10
TAB10_POSTGRES_PASSWORD=replace-me
TAB10_POSTGRES_DB=tab10
