{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "cd ../.. && npm install --global --prefix \"$HOME/.local\" node@24.20.0 pnpm@9.15.0 && export PATH=\"$HOME/.local/bin:$PATH\" && node --version && pnpm --version && pnpm install --frozen-lockfile --prod=false --filter \"@tab10/web...\"",
  "buildCommand": "cd ../.. && export PATH=\"$HOME/.local/bin:$PATH\" && node --version && pnpm --version && pnpm run build:web",
  "outputDirectory": "dist",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "*": false
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://one0is-ball.onrender.com/api/:path*"
    },
    {
      "source": "/health",
      "destination": "https://one0is-ball.onrender.com/health"
    },
    {
      "source": "/ready",
      "destination": "https://one0is-ball.onrender.com/ready"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
