# GAP-012 final original-order full browser

Date: 2026-09-19. The final authorized corrected-tree run used fresh disposable PostgreSQL 16.15, compiled API and Vite preview. It passed 60/60 browser scenarios plus 9/9 foundation checks with zero failed, skipped, todo or interrupted results.

The temporary R2 helper was passive. It observed the real suite fixture and did not route, hold, delay, retry or synthesize responses. Desktop and mobile directory groups committed before focus; focus opened the list and scrolled the input into bounds; input filtered the existing options; pointer and mouse events preceded normal option removal and `aria-expanded` closing. Query and input identity were retained, with no blur, pre-click close, page error, dropped capture or unexpected transition.

`browser-summary.json` is the run summary. `final-capture-classification.json` records the two capture hashes and classification. `passive-helper-provenance.json`, the R1/R2 sources and diff preserve the temporary helper review. `temporary-passive-hook.patch` is evidence only; the helper was removed after the run and `tests/e2e/wave-e-user.spec.ts` was restored to its exact base hash.

The earlier isolated desktop timeout cause remains unresolved. This green run is not evidence that it was harmless, pre-existing or fixed. Physical keyboard/safe-area, WebKit and spoken assistive technology remain outside this run.
